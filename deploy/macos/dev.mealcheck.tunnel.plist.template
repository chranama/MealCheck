<!--
RETIRED COMPATIBILITY NOTICE

This path records the original MealCheck-owned tunnel bootstrap artifact. It is intentionally not
an installable plist. The canonical non-secret LaunchDaemon template and shared-ingress runbook are
owned by:

  https://github.com/chranama/web-server-infrastructure

The server promotes a reviewed commit into:

  /Users/chranama-server/web-server-infrastructure-runtime/installed/

Ordinary MealCheck installation, release, and restart operations must not replace or restart the
shared cloudflared service.
-->
